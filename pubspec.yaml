name: flash_order
description: "A fast food ordering and analytics platform"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  uuid: ^4.3.3
  intl: ^0.19.0
  cupertino_icons: ^1.0.8
  firebase_core: ^3.10.1
  cloud_firestore: ^5.6.1
  fl_chart: 0.69.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  # We use version 4.0.0 here to ensure compatibility with your SDK
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true